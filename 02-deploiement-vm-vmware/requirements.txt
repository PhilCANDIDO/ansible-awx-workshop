# Requirements Python pour l'atelier VMware
# Ces packages sont nécessaires pour interagir avec vCenter via Ansible

# PyVmomi : SDK Python officiel pour VMware vSphere
# IMPORTANT : Version fixée à 8.0.3.0 pour compatibilité avec community.vmware 4.x
# La version 9.0.0+ introduit des breaking changes non supportés
pyvmomi==8.0.3.0

# Requests : Pour les appels HTTP/HTTPS vers vCenter
requests>=2.28.0

# URLlib3 : Gestion des connexions HTTPS
urllib3>=1.26.0

# Jinja2 : Pour les templates Ansible
Jinja2>=3.0.0

# PyYAML : Pour parser les fichiers YAML
PyYAML>=6.0

# Cryptography : Pour Ansible Vault
cryptography>=41.0.0